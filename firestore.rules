rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /davinci/control-caja {
      allow read, write: if request.auth != null;
    }
    match /davinci/control-caja/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
